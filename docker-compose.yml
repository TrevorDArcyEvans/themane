version: '3.4'

services:
  themane.web:
    image: themane.web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
      - "80:80"
    environment:
      - ASPNETCORE_ENVIRONMENT
